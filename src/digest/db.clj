(ns digest.db
  (:require [monger.core :as mcore]))

(let []
  (into-array-list [1]))
  

;(defn connect [] 
;  (mcore/connect))

;(defn get-db [conn])
  ;(mcore/use-db conn "digest_db"))

;(defn disconnect [])
  ;(mcore/disconnect conn))

